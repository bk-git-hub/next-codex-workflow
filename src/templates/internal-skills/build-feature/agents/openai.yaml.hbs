interface:
  display_name: "Build Feature"
  short_description: "Implement the plan through executor, verifier, and reviewer"
  default_prompt: "Use $build-feature to implement the approved plan in PLAN.md by spawning the agent named executor immediately, telling it to use the relevant repo-local quality skills under .agents/skills/, then spawn verifier and reviewer, and stop if executor spawning is unavailable."

policy:
  allow_implicit_invocation: false
