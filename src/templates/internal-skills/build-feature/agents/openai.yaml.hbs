interface:
  display_name: "Build Feature"
  short_description: "Implement the plan with subagents"
  default_prompt: "Use $build-feature to implement the approved plan in PLAN.md, use subagents where helpful, and finish with verification and review."

policy:
  allow_implicit_invocation: false
