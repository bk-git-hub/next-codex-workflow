<!-- Generated by next-codex-workflow-kit -->
# Repository Overview

Detected router style: {{routerStyle}}

Important source roots:
{{sourceRoots}}

Workflow artifacts live in `agent-workflow/artifacts/`.

Current workflow mode: {{workflowModeLabel}}
Automatic workflow commits: {{autoCommitLabel}}

# Commands

{{commands}}

# Workflow Shortcuts

- `$plan-feature` to {{planShortcutDescription}}
- `$build-feature` to {{buildShortcutDescription}}
- `$verify-feature` to {{verifyShortcutDescription}}
- `$review-feature` to {{reviewShortcutDescription}}

# Workflow Artifacts

{{workflowArtifacts}}

# Feature Work Routing Rules

1. For non-trivial feature work, do not write implementation code before `PLAN.md` and `FILE_SPECS.md` exist.
2. Use the planning workflow before introducing new routes, server/client boundaries, state ownership changes, or major UI composition changes.
3. If more than one viable strategy exists, ask the user to choose.
4. After a user decision, update `DECISION.md`.
5. {{planningRoutingRule}}
6. {{implementationRoutingRule}}
7. After implementation, run verification and update `VERIFY.md`.
8. After verification, run structured review and update `REVIEW.md`.
9. {{autoCommitRule}}
{{performanceRule}}

# Definition of Done

- Implementation completed.
- Tests updated or deliberately left unchanged with rationale.
- Verification attempted.
- Review completed.
- Decisions logged when required.
- Remaining risks called out explicitly.
